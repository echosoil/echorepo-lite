services:
  echorepo-lite:
    # no container_name here; let Compose generate it
    environment:
      APP_ENV: dev
      FLASK_DEBUG: "1"
    ports:
      - "127.0.0.1:18080:8000"   # only loopback on the server

  postgres:
    # dev can keep 5432 on host
    ports:
      - "5432:5432"